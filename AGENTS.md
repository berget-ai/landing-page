# Agents

## Local Development Setup

If the `@berget-ai/ui` repo is checked out at `../ui`, follow the instructions in `.opencode/skills/setup-local-dev.md` to link the local UI package before starting development. This ensures you are developing against the latest local version of `@berget-ai/ui`.
